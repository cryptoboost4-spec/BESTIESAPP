[1mdiff --git a/firebase.json b/firebase.json[m
[1mindex 992dd3e..d053229 100644[m
[1m--- a/firebase.json[m
[1m+++ b/firebase.json[m
[36m@@ -27,7 +27,7 @@[m
           "headers": [[m
             {[m
               "key": "Content-Security-Policy",[m
[31m-              "value": "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://*.firebaseapp.com https://*.googleapis.com https://www.googletagmanager.com https://apis.google.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https: blob:; connect-src 'self' https://*.googleapis.com https://*.firebaseio.com wss://*.firebaseio.com https://www.googletagmanager.com; frame-src 'none'; object-src 'none'; base-uri 'self'; form-action 'self';"[m
[32m+[m[32m              "value": "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://*.firebaseapp.com https://*.googleapis.com https://www.googletagmanager.com https://apis.google.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https: blob:; connect-src 'self' https://*.googleapis.com https://*.firebaseio.com wss://*.firebaseio.com https://www.googletagmanager.com; frame-src 'self' https://*.firebaseapp.com https://*.googleapis.com https://accounts.google.com; object-src 'none'; base-uri 'self'; form-action 'self';"[m
             },[m
             {[m
               "key": "X-Content-Type-Options",[m
[1mdiff --git a/firestore.rules b/firestore.rules[m
[1mindex 4565066..ecd6ee1 100644[m
[1m--- a/firestore.rules[m
[1m+++ b/firestore.rules[m
[36m@@ -69,6 +69,12 @@[m [mservice cloud.firestore {[m
         (!request.resource.data.keys().hasAny(['role', 'banned', 'isAdmin']));[m
       allow delete: if isSignedIn() &&[m
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isAdmin == true;[m
[32m+[m
[32m+[m[32m      // Settings subcollection - user preferences and tutorial states[m
[32m+[m[32m      match /settings/{settingId} {[m
[32m+[m[32m        allow read: if isSignedIn() && request.auth.uid == userId;[m
[32m+[m[32m        allow write: if isSignedIn() && request.auth.uid == userId;[m
[32m+[m[32m      }[m
     }[m
     [m
     // Check-ins collection[m
[36m@@ -189,45 +195,35 @@[m [mservice cloud.firestore {[m
     // for error tracking (errors can occur before login). Rate limiting handled at function level.[m
     match /errors/{errorId} {[m
       // Allow authenticated users, or allow unauthenticated with timestamp validation (rate limiting)[m
[32m+[m[32m      // Increased from 60s to 5min for practical error handling while maintaining abuse prevention[m
       allow create: if isSignedIn() || [m
         (request.resource.data.timestamp != null && [m
[31m-         request.resource.data.timestamp > (request.time.toMillis() - 60000)); // Max 1 per minute for anonymous[m
[32m+[m[32m         request.resource.data.timestamp is number &&[m
[32m+[m[32m         request.resource.data.timestamp > (request.time.toMillis() - 300000)); // 5 minutes (300000ms)[m
       allow read: if isAdmin();[m
       allow update, delete: if false;[m
     }[m
     [m
     // Performance tracking collection[m
     match /performance/{perfId} {[m
[32m+[m[32m      // Allow authenticated users, or allow unauthenticated with timestamp validation (rate limiting)[m
[32m+[m[32m      // Increased from 60s to 5min for practical error handling while maintaining abuse prevention[m
       allow create: if isSignedIn() || [m
         (request.resource.data.timestamp != null && [m
[31m-         request.resource.data.timestamp > (request.time.toMillis() - 60000));[m
[32m+[m[32m         request.resource.data.timestamp is number &&[m
[32m+[m[32m         request.resource.data.timestamp > (request.time.toMillis() - 300000)); // 5 minutes (300000ms)[m
       allow read: if isAdmin();[m
       allow update, delete: if false;[m
     }[m
     [m
     // User actions tracking collection[m
[31m-    match /user_actions/{actionId} {[m
[31m-      allow create: if isSignedIn() || [m
[31m-        (request.resource.data.timestamp != null && [m
[31m-         request.resource.data.timestamp > (request.time.toMillis() - 60000));[m
[31m-      allow read: if isAdmin();[m
[31m-      allow update, delete: if false;[m
[31m-    }[m
[31m-[m
[31m-    match /performance/{perfId} {[m
[31m-      // Allow authenticated users, or allow unauthenticated with timestamp validation (rate limiting)[m
[31m-      allow create: if isSignedIn() || [m
[31m-        (request.resource.data.timestamp != null && [m
[31m-         request.resource.data.timestamp > (request.time.toMillis() - 60000)); // Max 1 per minute for anonymous[m
[31m-      allow read: if isAdmin();[m
[31m-      allow update, delete: if false;[m
[31m-    }[m
[31m-[m
     match /user_actions/{actionId} {[m
       // Allow authenticated users, or allow unauthenticated with timestamp validation (rate limiting)[m
[32m+[m[32m      // Increased from 60s to 5min for practical error handling while maintaining abuse prevention[m
       allow create: if isSignedIn() || [m
         (request.resource.data.timestamp != null && [m
[31m-         request.resource.data.timestamp > (request.time.toMillis() - 60000)); // Max 1 per minute for anonymous[m
[32m+[m[32m         request.resource.data.timestamp is number &&[m
[32m+[m[32m         request.resource.data.timestamp > (request.time.toMillis() - 300000)); // 5 minutes (300000ms)[m
       allow read: if isAdmin();[m
       allow update, delete: if false;[m
     }[m
